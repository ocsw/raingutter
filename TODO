TODO

multiples
updates
validate configs - incl. drupal chains
configs for OCS fields
testing

key list length

store chain_type?

nori import

drupal read:
    add node -> title, fc -> label, etc.
    add multi-field/node? vs at least note re multi-value vs multi-column
    inner joins for relations, etc?
    OR IS NULL cases

refs to rg funcs from config

---

redundant output log messages?

db/ssh conn msgs: identifiers

postgres table list
callable check

no_replicate for updates
    SET sql_log_bin=0;
    SET sql_log_bin=1;
    but - check/store

more examples/sql, esp for drupal

---

docstring / USAGE / mod notes re chains

error handling: exit/skip?
