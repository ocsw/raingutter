TODO

inserts
inner joins for relations, etc?
configs for OCS fields
testing

get last id - M: LAST_INSERT_ID() / cur.lastrowid  P: lastval()

pre/post callbacks, readonlymode (site_readonly 1/0)

nori import

refs to rg funcs from config

---

redundant output log messages?

db/ssh conn msgs: identifiers

postgres table list
callable setting check

no_replicate for updates
    SET sql_log_bin=0;
    SET sql_log_bin=1;
    but - check/store

more examples/sql, esp for drupal?

---

handle non-unique field titles, fc labels, relations
    probably need to have selects pass IDs, deltas back
    for labels, will need to select by delta in the getter
    for updates, probably need to limit by delta and/or prev. value

handle interpreting drupal field defaults

docstring / USAGE / mod notes re chains

error handling: exit/skip?

autocommit options

node -> title, fc -> label, etc. (?)
